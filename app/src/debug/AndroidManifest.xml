<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
            Debug-only auditor. Exported so it can be launched with `am start`
            without going through the launcher.
        -->
        <activity
            android:name=".debug.SignalAuditActivity"
            android:exported="true"
            android:label="Signal audit" />
    </application>

</manifest>
